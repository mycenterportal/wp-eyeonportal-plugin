---
description: EyeOn platform architecture, sibling repos, cross-repo edit protocol
alwaysApply: true
---

# EyeOn Platform Architecture

## You Are Here

Current workspace is **wp-eyeonportal-plugin** (WordPress). Ongoing
first-class public platform for centers on WordPress; consumes
web-app-backend via API token.

The EyeOn portal product spans multiple sibling applications. Always reason
about a change with these boundaries in mind and implement end-to-end across
all affected repos in a single session using the absolute paths below.

## Cross-Repo Edit Protocol

When implementing a feature in this session:

1. Identify all affected sibling repos from the table below.
2. Make changes directly in each repo using absolute paths — do not stop at
   the current workspace.
3. Schema/migrations: only in `admin-portal-backend/db/migrations/`.
4. Admin UI + staff flows: `admin-portal` + `admin-portal-backend`.
5. Public/read surface: `web-app-backend` (+ `center-website-runtime`, WP
   plugin, or kiosk when applicable).
6. Center website builder/runtime: `center-website` + `admin-portal-backend` +
   `web-app-backend`.
7. Never add business logic to public frontends; keep writes in
   `admin-portal-backend`.
8. Never invent APIs — locate or add endpoints in the correct backend first.

## Product Roles

| Role | App | Responsibility |
|---|---|---|
| Entry | `admin-portal` | Staff dashboard; "Manage Website" magic link into builder |
| Admin API | `admin-portal-backend` | Protected REST API; owns Postgres schema |
| Public API | `web-app-backend` | Read-only API for public consumers (API token) |
| Builder | `center-website` | Author pages, sections, menus, domains, preview |
| Runtime | `center-website-runtime` | Serve published/test domains via `web-app-backend` |
| WordPress | `wp-eyeonportal-plugin` | WordPress/Elementor public sites (ongoing platform) |
| Kiosk | `kiosk-electron` | Touch kiosk + media-display (ads) for centers |
| Map library | `threejs-map` | React/Three.js `@eyeon/threejs-map`; renders center maps from SVG floors + mapit2 data |
| Helpdesk | `eyeon-helpdesk` | Internal support ticketing; clients submit/reply via web or email |

## Public Website Platforms (coexistence)

Centers may use **either or both** public website stacks long term:

| Platform | Stack | Status |
|---|---|---|
| WordPress + plugin | `wp-eyeonportal-plugin` | **Ongoing** — many clients keep WordPress for non-EyeOn site features |
| Center website | `center-website` + `center-website-runtime` | **Growing** — new and migrating centers; staging today |

- Both platforms consume the same center data via `web-app-backend` (API token).
- **Do not assume** all centers move to center-website. Full migration may
  happen over 2–3 years but is **not guaranteed**.
- Public API or data-shape changes must consider **both** WordPress plugin and
  center-website-runtime consumers unless explicitly scoped to one platform.

## Sibling Projects (always in scope)

| Project | Stack | Path | Role |
|---|---|---|---|
| `admin-portal` | React | `/Users/angrej/work/eyeon/eyeonportal/admin-portal/` | Staff dashboard where users manage mall center data. Generates API access tokens for public apps. "Manage Website" magic-link entry into center-website builder. |
| `admin-portal-backend` | Node.js (Express) | `/Users/angrej/work/eyeon/eyeonportal/admin-portal-backend/` | Protected business/admin REST API. Owns the shared Postgres schema and all migrations. Consumed by `admin-portal` and `center-website`. |
| `web-app-backend` | Node.js | `/Users/angrej/work/eyeon/eyeonportal/web-app-backend/` | Public **read-only** API. Same DB via AWS RDS read replica. Serves public apps authenticated with API access tokens (generated in admin-portal by super admin). |
| `center-website` | Next.js | `/Users/angrej/work/eyeon/eyeonportal/center-website/` | Website builder UI. Calls `admin-portal-backend` via session cookie (`/eyeon/api/*` proxy and `/api/*` route handlers). Staging only; not production yet. **Center Map** section (`centerMap`) renders via `@eyeon/threejs-map`. |
| `center-website-runtime` | Next.js | `/Users/angrej/work/eyeon/eyeonportal/center-website/runtime/` | Serves test and live domains. Renders websites built in center-website, including the **Center Map** section via shared `@eyeon/threejs-map` `MapBox`. Data via `web-app-backend` `/v1/runtime/*`. |
| `kiosk-electron` | Electron | `/Users/angrej/work/eyeon/eyeonportal/kiosk-electron/` | Desktop app for Linux/Windows. Two display modes: **kiosk** (touch-interactive) and **media-display** (ads uploaded in admin-portal). Uses API access token. Renders center map via `@eyeon/threejs-map`. |
| `threejs-map` | React / Three.js (npm lib) | `/Users/angrej/work/eyeon/eyeonportal/threejs-map/` | Published as `@eyeon/threejs-map`. Converts SVG floor plans into interactive 3D maps; loads retailer/amenity assignments from mapit2 APIs. Consumed by admin-portal Center Map editor, center-website Center Map section, center-website-runtime, kiosk-electron, and WP plugin bundled releases. |
| `wp-eyeonportal-plugin` | WordPress / PHP / Elementor | `/Users/angrej/Sites/wp-eyeonportal/wp-content/plugins/eyeonportal/` | WordPress plugin serving center data from `web-app-backend` via Elementor widgets. First-class public platform alongside center-website-runtime. Map widget loads prebuilt JS/CSS from `assets/map-releases/` synced by threejs-map CI. |
| `eks-iac` | Terraform + K8s YAML | `/Users/angrej/work/eyeon/eyeonportal/eks_iac/` | Deploys EyeOn apps to AWS EKS (`SAS-IT-LABS`, `us-east-2`). Staging and prod namespaces. |
| `eyeon-helpdesk` | Next.js | `/Users/angrej/work/eyeon/eyeonportal/eyeon-helpdesk/` | Standalone support ticketing app. Internal team manages tickets at `/admin/*`; clients submit via `/contact` or reply by email. Own Postgres DB (dbmate). Deployed on **AWS Amplify** (not EKS). |

## App Boundaries (default ports)

- Admin Portal UI: `localhost:3000` (`admin-portal`)
- Admin APIs (protected): `localhost:3001` (`admin-portal-backend`)
- Public APIs (token protected): `localhost:3002` (`web-app-backend`)
- Center builder app: `localhost:3004` (`center-website`)
- Center runtime app: `localhost:3005` (`center-website/runtime`)
- Helpdesk app: `localhost:3006` (`eyeon-helpdesk`; note: `package.json` dev script currently uses `3004` — use `3006` locally if center-website is also running)

## Auth

- **Admin flows:** session/cookie → `admin-portal-backend` (staff login via admin-portal).
- **Public consumers:** API access token → `web-app-backend` (WP plugin, kiosk, runtime).
- API access tokens are generated in admin-portal by super admin.
- **Admin Portal → Builder (Magic Link SSO**, Redis in `admin-portal-backend`):
  - Create: `POST /v1/center-website/magic-link/create` with `{ center_id, redirect_path? }`
    (default `redirect_path`: `/websites/:center_id`)
  - Consume: `POST /v1/public/center-website/magic-link/consume` (builder sets session
    cookie, redirects to `redirectPath`)
  - Requires matching `MAGIC_LINK_SHARED_SECRET` and `CENTER_WEBSITE_BASE_URL` on
    `admin-portal-backend` and the builder
  - Do not re-introduce local auth UI in center-website (`/login`, `/register`, etc.)

## Database / Migrations Policy

- Shared business schema lives in AWS PostgreSQL RDS.
- `admin-portal-backend` uses the primary DB; `web-app-backend` uses the read replica.
- **All migrations** live in `admin-portal-backend/db/migrations/` only.
- No other repo owns shared schema migrations.

## Cross-Repo Impact Guide

| Change type | Repos to update |
|---|---|
| New DB column/table | `admin-portal-backend` (migration + admin API), then consumers |
| Staff UI for new field | `admin-portal` + `admin-portal-backend` |
| Public read of new field | `web-app-backend` (+ runtime **and** WP plugin if exposed publicly) |
| Center website section | `center-website` + `admin-portal-backend` + `web-app-backend`; align with WP plugin widgets where sections mirror each other; **Center Map** section also needs `threejs-map` |
| WordPress widget / Elementor feature | `wp-eyeonportal-plugin` + `web-app-backend` (+ admin-portal if staff configures it) |
| Magic link / Manage Website | `admin-portal` + `admin-portal-backend` + `center-website` |
| Kiosk display feature | `kiosk-electron` + `web-app-backend` (+ admin-portal if staff configures it) |
| Center map (floors, retailers, amenities, settings) | `threejs-map` + `admin-portal` + `admin-portal-backend` + `web-app-backend`; release bumps consumers (`center-website`, `center-website-runtime`, `kiosk-electron`, WP plugin via CI) |
| Map library UI/behavior change | `threejs-map` (publish npm) + bump `@eyeon/threejs-map` in admin-portal, center-website, kiosk-electron; runtime inherits center-website section dep; WP gets bundled assets on threejs-map release |
| API token scopes | `admin-portal` + `admin-portal-backend` + `web-app-backend` |
| EKS / deploy config | `eks_iac` + affected app repo |
| Helpdesk feature / schema | `eyeon-helpdesk` only (standalone DB + Next.js API routes) |

## EyeOn Helpdesk (`eyeon-helpdesk`)

Standalone Next.js 15 ticketing system for EyeOn client support. **Not**
connected to the shared `admin-portal-backend` Postgres schema — it owns its
own database and migrations.

### Purpose

- **Internal team** (support agents, leads, superadmins) log in at `/admin/login`
  and manage tickets, customers, companies, and settings.
- **Clients** (mall/center contacts) report issues or request work via the
  public `/contact` form, or by replying to ticket notification emails.
- Email replies are ingested via an AWS SES inbound webhook and appear in the
  ticket conversation thread alongside web replies.

### Architecture

| Layer | Details |
|---|---|
| UI | Next.js App Router — admin at `/admin/*`, public at `/contact` and `/ticket/[ticketNumber]` |
| API | Next.js Route Handlers under `/api/admin/*` (JWT cookie) and `/api/public/*` (Turnstile on writes) |
| Auth | JWT in `eyeon_admin_auth_token` httpOnly cookie; roles: `superadmin`, `support_lead`, `support_agent` |
| Database | Dedicated PostgreSQL; migrations in `eyeon-helpdesk/db/migrations/` via **dbmate** (not admin-portal-backend) |
| Email | AWS SES outbound (notifications) + inbound SNS webhook at `/api/webhooks/ses/incoming` |
| Storage | S3 for ticket attachments |
| AI (optional) | OpenAI for categorization, spam detection, contact extraction, reply analysis |

### Session handling

When the admin JWT expires while a user is still in the browser, API calls
return `401`. Client-side `adminFetch` / Axios interceptors redirect to
`/admin/login` on any `/api/admin/*` 401 (excluding `/api/admin/auth/*`).

### Deploy (AWS Amplify)

- CI/CD: `amplify.yml` — build, run `db:prod:up` migrations, Teams webhook on success/failure.
- Branches: `main` → prod, `staging` → stage.
- **Not** deployed via `eks_iac` today. See cost notes below.

### Amplify vs EKS (helpdesk)

Other EyeOn apps share an existing EKS cluster (`eks_iac`). Helpdesk is on
Amplify as a cost/experiment choice. Rule of thumb:

- **Stay on Amplify** if traffic is low (internal team + occasional client
  form/email), you want zero cluster ops, and build minutes stay within free/low
  tier. Amplify Hosting + SSR is often **$5–30/mo** for a small internal app.
- **Move to EKS** if the cluster already has spare node capacity (marginal cost
  ≈ one small Deployment + Service — often cheaper than paying Amplify build +
  SSR separately at scale), you need VPC-private DB access patterns aligned with
  other services, or you want one ingress/ALB and unified observability.
- EKS **fixed** costs (~$73/mo control plane + nodes) are already sunk if the
  cluster runs either way; adding helpdesk to EKS is mainly incremental pod
  memory/CPU, not a second platform fee.

Decision: keep Amplify while validating product fit and traffic; revisit EKS
when consolidating deploys or if Amplify build/hosting bills exceed ~$40–50/mo.

## Center Map (`threejs-map`)

`threejs-map` is the shared React library (`@eyeon/threejs-map` on npm) that
powers interactive center maps across the platform.

### Data flow

1. **SVG floors** — staff upload floor SVG files in admin-portal **Center Map**
   (`CENTER_MAP:FLOORS`). Stored in `mapit2_floors` (S3-backed `svg` key) via
   `admin-portal-backend` `/v1/mapit2/floors`.
2. **Map configuration** — staff assign retailers, amenities, kiosks, routes,
   and map settings in admin-portal Center Map (`ROLE: PORTAL`). Writes go to
   `mapit2_data`, `mapit2_settings`, `mapit2_responsive`, etc. via
   `admin-portal-backend` `/v1/mapit2/*`.
3. **Public read** — frontends fetch map data from `web-app-backend`
   `/v1/mapit2/data`, `/floors`, `/settings`, `/responsive` (API token).
4. **Render** — `@eyeon/threejs-map` `MapBox` parses SVG layers, builds 3D
   geometry, and overlays retailer/amenity assignments.

### Consumers

| App | Role / mode | Notes |
|---|---|---|
| `admin-portal` | `PORTAL` | Center Map module (`/center-map`) — primary editor for floors + assignments |
| `center-website` | public section | `CenterMap` section (`src/public/sections/CenterMap`); builder preview proxies map API |
| `center-website-runtime` | public section | Same `CenterMap` section on published/test domains via shared `section-map` loaders |
| `kiosk-electron` | kiosk mode | Touch map + amenities; webpack aliases ESM build |
| `wp-eyeonportal-plugin` | `WP_SITE` | Elementor map widget; loads bundled `main.js` + `main.css` from `assets/map-releases/{version}/` |

### Release pipeline

On push to `threejs-map` `main` (version bump required):

1. Build library → publish `@eyeon/threejs-map` to npm.
2. Build `threejs-wp/` wrapper → copy `main.js`, `main.css`, source map to
   `wp-eyeonportal-plugin` repo at `assets/map-releases/{version}/`.
3. Commit to `mycenterportal/wp-eyeonportal-plugin` (`ThreeJS Map: {version} [skip ci]`).

Bump `@eyeon/threejs-map` in admin-portal, center-website, and kiosk-electron
when adopting a new library version. WP plugin picks up bundled assets
automatically from the CI push above.

## WordPress Plugin (`wp-eyeonportal-plugin`)

The WP plugin is an **ongoing first-class platform**, not a deprecated stack.

- Many centers keep WordPress because clients use it for non-EyeOn features.
- Plugin Elementor widgets and center-website sections may mirror the same
  center data — keep public API contracts compatible for both.
- When building center-website sections (`section-registry.ts`, `src/public/`),
  treat matching Elementor widgets as a **parity reference** for UX, props, and
  data-fetch patterns — do not import PHP into other repos.
- When changing public data used by widgets, update **both** the plugin and
  `web-app-backend` (and center-website-runtime if the same data is exposed there).

## Center Website (builder + runtime)

### Cross-repo rules

- Backend API changes → `admin-portal-backend` (never business logic in builder/runtime).
- Published runtime contract changes → `web-app-backend` must match what
  `center-website-runtime` reads.
- Manage Website / magic link / redirect changes → `admin-portal` +
  `admin-portal-backend` + `center-website` together.

### Website lifecycle (1:1 with `centers`)

The `websites` table has `UNIQUE (center_id)` — at most one website per center.

**Creation contract — user consent required.** Website rows must only be created
via an explicit "Build Website" action, never on read or page-load flows.

| Endpoint | Behavior |
|---|---|
| `GET /v1/websites/by-center/:center_id` | Read-only; `404` if no website yet |
| `POST /v1/websites/by-center/:center_id` | Explicit init; idempotent; seeds defaults |

Builder proxies: `GET/POST /eyeon/api/v1/websites/by-center/:centerId`.

After Build Website, `seedWebsiteDefaults` (center-website) seeds via builder
write APIs: default page tree (Home + locked pages per module — Stores, Dine,
Map, Deals, Events, News, Careers), default sections from `section-registry.ts`,
header/footer menus in `website_settings`, and a preview test subdomain
(`is_preview`). Banners global colors live in `web_slider_settings` (`center_id`)
via admin-portal, not `website_settings`. Re-invocations only fill empty slots;
user edits are never overwritten. `ensureForCenter` resolves `website_id` lazily
but does **not** seed.

**UI gate:** `center-website/src/components/builder/ProjectShell.tsx` — on mount
read-only fetch; `404` shows "not initialized" + Build Website button (builder
children not mounted); Build Website POSTs then redirects to `/websites/:id/preview`.
"Manage Website" from admin-portal lands at `/websites/:id` (center ID).

### Deploy (eks-iac)

Manifests: `eks_iac/K8s/center-website-builder/` and `center-website-runtime/`.

| Tier | Builder host | Runtime test domains |
|---|---|---|
| Staging | `builder-staging.eyeonportal.com` | `{slug}-staging.eyeontest.com` |
| Production | `builder.eyeonportal.com` | `{slug}.eyeontest.com` |

- Runtime has no `DATABASE_URL`; bootstraps via `web-app-backend` `/v1/runtime/*`
  using `RUNTIME_SERVICE_SECRET`.
- `web-app-backend` staging needs `TEST_DOMAIN_SUFFIX=eyeontest.com`.
- Stored `website_domains.domain` prefix: `{slug}-staging` (staging) or `{slug}` (prod).
- **ALB gotcha:** while prod center-website is not deployed, prod ingress must not
  declare `*.eyeontest.com` or `builder.eyeonportal.com` — a missing backend rule
  returns ALB 503 and blocks staging. See `eks_iac/K8s/ingress-rules/prod-alb/ingress.yaml`.
- Prod go-live: `eks_iac/docs/center-website-prod-go-live.md`.

## Documentation Sync

When platform boundaries change, update:

- `admin-portal/docs/cursor-rules/eyeonportal-platform-architecture.mdc` (canonical)
- Run `npm run sync:cursor-rules` from `admin-portal` to propagate
